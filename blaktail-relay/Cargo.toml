[package]
name = "blaktail-relay"
description = "Australia-pinned BlakTail UDP relay"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "blaktail-relay"
path = "src/main.rs"

[dependencies]
blaktail-config = { path = "../blaktail-config" }
hmac = "0.12"
sha2 = "0.10"
clap = { version = "4", features = ["derive", "env"] }
tokio = { version = "1", features = ["macros", "io-util", "net", "rt-multi-thread", "signal", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
