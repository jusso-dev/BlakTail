{
  "name": "blaktail",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/console"
  ],
  "scripts": {
    "lint": "bun --filter @blaktail/console lint",
    "typecheck": "bun --filter @blaktail/console typecheck",
    "build": "bun --filter @blaktail/console build",
    "dev:console": "bun --filter @blaktail/console dev"
  }
}
